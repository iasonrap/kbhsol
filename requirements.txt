# This project is otherwise stdlib-only Python (see CLAUDE.md). h5py is the
# one deliberate exception, needed only by /api/precipitation to decode
# DMI's radar composite (ODIM_H5 format) — there's no stdlib way to read
# HDF5. Pulls in numpy as its own transitive dependency; nothing else here
# imports numpy directly.
h5py>=3.9
